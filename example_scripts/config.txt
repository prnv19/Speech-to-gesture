gpu=0                 # which GPU to use to run the model
folder=dataset       # which folder with the data to use
data_dir=/home/ubuntu/Speech-to-gesture/$folder
speech_features=MFCC # which speech features to use
numb_in_features=26  # how many speech features there are
dim=384              # what is the dimensionality of representation
